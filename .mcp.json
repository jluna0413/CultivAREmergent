{
  "providers": {
    "ollama": {
      "type": "local",
  "baseUrl": "${OLLAMA_BASEURL:-http://localhost:11435}",
      "apiKey": "${OLLAMA_API_KEY:-}",
      "description": "Project-level overrides for Ollama. Keep secrets out of git; set OLLAMA_BASEURL or OLLAMA_API_KEY in your environment or in a local .env file (gitignored)."
    }
  },
  "mcpServers": {
    "taskmaster-ai": {
      "env": {
        "OLLAMA_API_KEY": "${OLLAMA_API_KEY:-}",
  "OLLAMA_BASEURL": "${OLLAMA_BASEURL:-http://localhost:11435}"
      }
    }
  }
}
